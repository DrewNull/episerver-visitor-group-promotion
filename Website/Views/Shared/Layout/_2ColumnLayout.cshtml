@{
    Layout = "~/Views/Shared/Layout/_Layout.cshtml";
}

<div class="container">
    <div class="row">
        <div class="col-8">
            @RenderBody()
        </div>
        <div class="col-4">
            @if (IsSectionDefined("Right"))
            {
                @RenderSection("Right")
            }
        </div>
    </div>
</div>
